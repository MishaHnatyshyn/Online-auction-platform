import React from 'react';
import SectionTitle from '../SectionTitle'

export default function ThirdSection() {
  return(
    <section className="content-section">
      <div>
        <SectionTitle className="left" text="Third section"/>
      </div>
    </section>
  )
}
