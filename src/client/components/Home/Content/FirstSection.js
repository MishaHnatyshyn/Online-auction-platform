import React from 'react';
import SectionTitle from '../SectionTitle'

export default function FirstSection() {
  return(
    <section className="content-section">
      <div>
        <SectionTitle className="left" text="First section"/>
      </div>
    </section>
  )
}
